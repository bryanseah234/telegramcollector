# ============================================
# TELEGRAM API CREDENTIALS [REQUIRED]
# ============================================
# How to get these:
# 1. Go to https://my.telegram.org
# 2. Log in with your phone number
# 3. Click "API development tools"
# 4. Create an application (any name works)
# 5. Copy the api_id and api_hash

TG_API_ID=12345678
TG_API_HASH=abcdef1234567890abcdef1234567890

# ============================================
# BOT TOKEN [REQUIRED]
# ============================================
# How to get this:
# 1. Open Telegram and search for @BotFather
# 2. Send /newbot and follow the prompts
# 3. Copy the bot token (looks like 123456:ABC-DEF...)
#
# This bot is used for:
# - User account registration (users send /start to log in)
# - Topic management in the hub group
# - Publishing media to topic threads

BOT_TOKEN=123456789:ABCdefGHIjklMNOpqrsTUVwxyz


# ============================================
# HUB GROUP [REQUIRED]
# ============================================
# This is the Telegram group where face topics will be created
#
# How to set up:
# 1. Create a new Telegram group
# 2. Go to Group Settings → Topics → Enable "Topics"
# 3. Make sure your account is an admin
# 4. Forward any message from the group to @userinfobot
# 5. Copy the ID (starts with -100, like -1001234567890)

HUB_GROUP_ID=-1001234567890

# ============================================
# DATABASE CONFIGURATION [REQUIRED]
# ============================================
# If using Docker (recommended), just set the password
# The other values will work with docker-compose defaults

DB_HOST=postgres
DB_PORT=5432
DB_NAME=face_archiver
DB_USER=postgres
DB_PASSWORD=CHANGE_THIS_TO_A_SECURE_PASSWORD

# ============================================
# PROCESSING CONFIGURATION
# ============================================
# These have sensible defaults - only change if needed

# Face similarity threshold (0.0 to 1.0)
# - 0.55 = good balance of accuracy and matching
# - Lower = more matches but possible false positives
# - Higher = fewer matches but more accurate
SIMILARITY_THRESHOLD=0.55

# Minimum face quality to process (0.0 to 1.0)
# Faces below this threshold are ignored
MIN_QUALITY_THRESHOLD=0.5

# Maximum media file size in MB
# Files larger than this are skipped
MAX_MEDIA_SIZE_MB=50

# Number of worker threads for processing
NUM_WORKERS=3

# GPU acceleration (true/false)
# Set to true if you have NVIDIA GPU with CUDA
USE_GPU=false

# ============================================
# RUN MODE
# ============================================
# Run mode: backfill, realtime, or both
RUN_MODE=both

# ============================================
# HEALTH MONITORING
# ============================================
# How often to run health checks (in seconds)
# 1800 = every 30 minutes
HEALTH_CHECK_INTERVAL=1800

# ============================================
# AUTO-UPDATE (OPTIONAL)
# ============================================
# Set these to enable automatic updates from GitHub
# Leave empty to disable auto-updates

GITHUB_REPO=
GITHUB_BRANCH=main
UPDATE_CHECK_INTERVAL=1800

# ============================================
# LOGGING
# ============================================
# Log level: DEBUG, INFO, WARNING, ERROR
LOG_LEVEL=INFO

# ============================================
# DOCKER CONFIGURATION
# ============================================
# Prefix for container names (default: face_archiver)
# Change this if running multiple instances or prefer custom names
COMPOSE_PROJECT_NAME=telegramcollector
